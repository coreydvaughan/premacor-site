<section class="section section-band section-band--dark problem">
  <span class="social-rail-end-anchor" aria-hidden="true"></span>
  <div class="container">
    <div class="layout-split layout-split--media-right">
      <div class="layout-split__text section-accent">
        <div class="problem-content">
          <p class="problem-eyebrow">What we hear from buyers</p>
          <h2>Precision at scale is a partner problem</h2>

          <p>
            When the design is sound but production still feels risky, the issue often isn’t the
            drawing—it’s knowing where to get <strong>reliable precision</strong> at a
            <strong>fair investment</strong>, and how to <strong>scale orders</strong> without new
            failures. Until that’s clear, every release feels like a bet: Will it match the spec?
            Will the next lot match the last?
          </p>

          <ul class="problem-points">
            <li>
              <span class="problem-points__lead">Finding the right shop</span>
              — It’s hard to tell who can truly hold tolerance and deliver value before you’re already
              committed.
            </li>
            <li>
              <span class="problem-points__lead">Scaling without surprises</span>
              — Moving from samples to steady production without drift, rework, or constant
              firefighting.
            </li>
            <li>
              <span class="problem-points__lead">Trust in spec and outcome</span>
              — You need tier-one execution you can plan around—parts that ship as promised, run
              after run.
            </li>
          </ul>
        </div>
      </div>

      <div class="layout-split__media">
        <figure class="media-frame">
          <img
            src="https://images.unsplash.com/photo-1504917595217-d002dc571841?auto=format&fit=crop&w=1400&q=80"
            alt=""
            width="700"
            height="525"
            loading="lazy"
          />
          <figcaption class="media-frame__caption">Stock — replace with your floor</figcaption>
        </figure>
      </div>
    </div>
  </div>
</section>

<style>
  .problem {
    position: relative;
  }

  .social-rail-end-anchor {
    position: absolute;
    left: 0;
    top: 50%;
    width: 0;
    height: 0;
    pointer-events: none;
    visibility: hidden;
  }

  .problem-content {
    max-width: 52ch;
  }

  .problem-eyebrow {
    margin: 0 0 0.65rem;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .problem-points {
    margin-top: var(--space-element);
    list-style: none;
  }

  .problem-points li {
    margin-bottom: 1rem;
    color: var(--text-secondary);
    line-height: 1.55;
  }

  .problem-points__lead {
    display: inline;
    font-weight: 600;
    color: var(--text-primary);
  }
</style>
