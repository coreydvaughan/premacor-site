<section class="section section-band section-band--light-alt testimonial">
  <div class="container">
    <div class="testimonial-inner">
      <p class="testimonial-eyebrow">What partners say</p>
      <blockquote class="testimonial-bar testimonial-content">
        <p class="testimonial-quote">
          Premacor delivers consistent, high-quality parts with the reliability required for
          production environments.
        </p>
        <footer>
          <cite class="testimonial-source">Manufacturing partner</cite>
        </footer>
      </blockquote>
    </div>
  </div>
</section>

<style>
  .testimonial-inner {
    max-width: min(720px, 100%);
  }

  .testimonial-eyebrow {
    margin: 0 0 1rem;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-eyebrow-on-band);
  }

  .testimonial-content {
    margin: 0;
    padding: 0;
    border: 0;
  }

  .testimonial-quote {
    margin: 0;
    font-size: clamp(1.15rem, 2vw, 1.35rem);
    line-height: 1.55;
    font-style: normal;
    color: var(--text-on-band);
  }

  .testimonial-source {
    display: block;
    margin-top: 1.25rem;
    font-size: 0.9rem;
    font-style: normal;
    color: var(--text-on-band-muted);
  }
</style>
