<section class="section section-band section-band--light certifications">
  <div class="container">
    <div class="certifications-head">
      <p class="cert-eyebrow">Quality & compliance</p>
      <h2>Certifications</h2>
      <p class="cert-lead">
        Drop your ISO and partner badges here—placeholders below show approximate size and rhythm.
      </p>
    </div>

    <div class="certifications-inner">
      <div class="placeholder-logo">Logo placeholder</div>
      <div class="placeholder-logo">Logo placeholder</div>
      <div class="placeholder-logo">Logo placeholder</div>
      <div class="placeholder-logo">Logo placeholder</div>
    </div>
  </div>
</section>

<style>
  .certifications-head {
    max-width: 52ch;
    margin-bottom: clamp(2rem, 4vw, 3rem);
  }

  .cert-eyebrow {
    margin: 0 0 0.65rem;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-eyebrow-on-band);
  }

  .cert-lead {
    margin-top: 0.75rem;
  }

  .certifications-inner {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-start;
    align-items: stretch;
    gap: clamp(1rem, 2vw, 1.5rem);
  }
</style>
