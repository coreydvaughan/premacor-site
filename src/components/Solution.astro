<section class="section section-band section-band--light solution">
  <div class="container">
    <div class="layout-split layout-split--media-left">
      <div class="layout-split__text section-accent">
        <div class="solution-content">
          <p class="eyebrow">How Premacor fits</p>
          <h2>Forty years of discipline, built for global production</h2>

          <p>
            Premacor turns proven part designs into <strong>stable, repeatable manufacturing</strong>.
            With <strong>40+ years</strong> of precision-machining experience and leadership respected in
            the trade, we pair deep process control with a capable team and efficient cost structure—so
            customers worldwide get <strong>inspection-ready parts</strong> at a
            <strong>fair price</strong>, without sacrificing spec.
          </p>

          <ul class="solution-points">
            <li>
              <span class="solution-points__lead">Engineering-first execution</span>
              — Process-controlled machining, clear standards, and verification geared toward long runs.
            </li>
            <li>
              <span class="solution-points__lead">Scale when you’re ready</span>
              — Systems and capacity aimed at growing your orders without reinventing the line each
              time.
            </li>
            <li>
              <span class="solution-points__lead">Global delivery, human craft</span>
              — Based in the Philippines, trusted by customers around the world—experienced people
              behind consistent output.
            </li>
            <li>
              <span class="solution-points__lead">Value that holds</span>
              — Top-tier quality at an investment that reflects real manufacturing discipline, not
              noise.
            </li>
          </ul>
        </div>
      </div>

      <div class="layout-split__media">
        <figure class="media-frame">
          <img
            src="/images/parts/parts-1.png"
            alt="Premacor precision machined parts"
            loading="lazy"
            decoding="async"
          />
        </figure>
      </div>
    </div>
  </div>
</section>

<style>
  /* Balanced split: keep image prominent without crushing copy width */
  .solution .layout-split {
    align-items: start;
  }

  .solution .layout-split__media {
    width: 100%;
    justify-self: start;
  }

  .solution .media-frame {
    min-height: clamp(280px, 40vw, 520px);
    width: min(100%, 760px);
  }

  @media (max-width: 960px) {
    .solution .media-frame {
      min-height: clamp(280px, 56vw, 520px);
      max-width: min(100%, 820px);
      margin-inline: auto;
    }
  }

  .solution-content {
    max-width: 56ch;
  }

  .solution-points {
    margin-top: var(--space-element);
    list-style: none;
  }

  .solution-points li {
    margin-bottom: var(--space-4);
    line-height: 1.55;
  }

  .solution-points__lead {
    display: inline;
    font-weight: 600;
  }
</style>
